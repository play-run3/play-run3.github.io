{
  "name": "bulk-url-checker",
  "version": "1.1.0",
  "private": true,
  "description": "Bulk HTTP status, redirect and response checker",
  "main": "server.js",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "dependencies": {
    "express": "^4.22.2"
  }
}
